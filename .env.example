# Web runtime. Empty uses the same-origin /api/market route.
NEXT_PUBLIC_MARKET_API_URL=
NEXT_PUBLIC_APP_ENV=development

# Capacitor architecture B: point the native shell at an HTTPS deployment.
# For a physical-device development session, use your computer's LAN URL.
CAPACITOR_SERVER_URL=https://taiwan-stock-farm.1203andyliang.workers.dev